<UserControl x:Class="Forgotten_Land_Launcher.Forms.Recovery_Form"
             xmlns:Spinners="clr-namespace:Forgotten_Land_Launcher.Elements.Spinners"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:properties="clr-namespace:Forgotten_Land_Launcher.Properties"
             xmlns:languages="clr-namespace:Forgotten_Land_Launcher.Languages"
             mc:Ignorable="d" RenderOptions.BitmapScalingMode="HighQuality"
             FontFamily="Fira Sans Condensed">
    <Grid>
        <Border Background="#D8000000" CornerRadius="0,0,0,9" HorizontalAlignment="Left" Width="400">
            <Grid>
                <Image Source="/Forgotten Land Launcher;component/Assets/logos/launcher_server_logo.png" HorizontalAlignment="Center" VerticalAlignment="Top" Width="100" Margin="0,25,0,0"/>
                <StackPanel Width="350" HorizontalAlignment="Center" Margin="0,150,0,50">
                    <Grid>
                        <Grid HorizontalAlignment="Center">
                            <TextBox x:Name="textBoxEmail" Style="{DynamicResource loginTextBox}" Text="{x:Static languages:Lang.login_window_form_textbox_register_email}" GotFocus="TextBox_GotFocus"/>
                            <Image Source="/Forgotten Land Launcher;component/Assets/Icons/icon_email_1_a.png" Width="14" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </Grid>
                        <Image x:Name="warningIconEmail" HorizontalAlignment="Right" VerticalAlignment="Center" Source="/Forgotten Land Launcher;component/Assets/icons/icon_warning_1_a.png" Width="18" Margin="0,0,20,0" Visibility="Hidden"/>
                    </Grid>
                    <Button x:Name="btnRecover" Style="{DynamicResource loginRegisterButton}" Content="{x:Static languages:Lang.login_window_button_send_recovery}" Margin="0,25,0,0" Click="btnRecover_Click"/>
                    <Viewbox x:Name="loginSpinner" Height="40" Margin="0,15,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                        <Spinners:Dotted_Spinner HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Viewbox>
                    <Button x:Name="btnCode" Style="{DynamicResource btnTosLink}" Content="{x:Static languages:Lang.login_window_button_other_have_reset_code}" Margin="0,15,0,0" Click="btnCode_Click"/>
                    <Grid Margin="0,20,0,0" Width="300">
                        <Border BorderThickness="0,0,0,1" BorderBrush="#FF3C3C3C" Width="70" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Static languages:Lang.login_window_form_divider_placeholder_2}" FontSize="14" Margin="0,-5,0,0" HorizontalAlignment="Center" Foreground="#FF848484" VerticalAlignment="Center"/>
                        <Border BorderThickness="0,0,0,1" BorderBrush="#FF3C3C3C" Width="70" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                    <StackPanel Margin="0,20,0,0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="loginButton" Style="{DynamicResource buttonStyle2}" Content="{x:Static languages:Lang.login_window_button_other_sign_in}" Click="loginButton_Click"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock VerticalAlignment="Bottom" HorizontalAlignment="Center" Foreground="#FFBDBDBD" Margin="0,0,0,25">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} {1}">
                            <Binding Source="{x:Static languages:Lang.login_window_powered_by}" />
                            <Binding Path="ServerName" Source="{x:Static properties:Settings.Default}" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
