<UserControl x:Class="Forgotten_Land_Launcher.Dialogs.Avatar_Settings_Dialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:languages="clr-namespace:Forgotten_Land_Launcher.Languages"
             mc:Ignorable="d" RenderOptions.BitmapScalingMode="HighQuality"
             Loaded="UserControl_Loaded">
    <Grid>
        <Border CornerRadius="7" Background="#F20E0E0E">
            <Border CornerRadius="7" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FF191919" MaxWidth="500" MinWidth="300">
                <StackPanel Margin="20,20,20,20">
                    <TextBlock Text="{x:Static languages:Lang.dialog_avatar_change_title}" Foreground="#FFCACACA" HorizontalAlignment="Center" FontWeight="Bold" FontSize="14"/>
                    <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                    <Border CornerRadius="50,50,50,50" BorderBrush="#FF404040" BorderThickness="1,1,1,1" Width="100" Height="100" Cursor="Hand" HorizontalAlignment="Left">
                        <Border.Background>
                            <ImageBrush x:Name="avatarHolder" ImageSource="/Forgotten Land Launcher;component/Assets/default_avatar.jpg" Stretch="UniformToFill"/>
                        </Border.Background>
                    </Border>
                        <StackPanel Orientation="Vertical" Margin="20,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{x:Static languages:Lang.dialog_avatar_change_text_picture_url}" Foreground="#FFB5B5B5" HorizontalAlignment="Left" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" VerticalAlignment="Center"/>
                            <TextBox x:Name="urlHolder" Text="Image url.." Width="300" Background="#FF444444" FontSize="14" Padding="3,3,3,3" BorderBrush="#FF636363" Foreground="#FFB5B5B5" Margin="0,10,0,0" TextChanged="urlHolder_TextChanged" Style="{DynamicResource loginTextBox}"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button x:Name="okButton" Style="{DynamicResource buttonStyle4}" Content="{x:Static languages:Lang.general_button_accept}" Height="25" Width="80" Background="#FF498628" Click="okButton_Click"/>
                        <Button x:Name="cancelButton" Style="{DynamicResource buttonStyle5}" Content="{x:Static languages:Lang.general_button_cancel}" Height="25" Width="80" Margin="15,0,0,0" Background="#FF862828" Click="cancelButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
