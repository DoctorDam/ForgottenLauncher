<Button x:Class="Forgotten_Land_Launcher.Elements.Buttons.GameButtonMore"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" x:Name="btnGameMoreOptions"
        Style="{DynamicResource btnGameMoreOptions}">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Width="30">
        <Border Width="22" Height="2" CornerRadius="1" Background="{Binding Foreground, ElementName=btnGameMoreOptions}"/>
        <Border Width="22" Height="2" CornerRadius="1" Background="{Binding Foreground, ElementName=btnGameMoreOptions}" Margin="0,3,0,0"/>
        <Border Width="22" Height="2" CornerRadius="1" Background="{Binding Foreground, ElementName=btnGameMoreOptions}" Margin="0,3,0,0"/>
    </StackPanel>
</Button>
